---
export interface Props {
    href?: string;
    width?: number;
    height?: number;
    inline?: boolean;
}

const { href, width, height, inline } = Astro.props;
---

<img class=`${inline ? "inline-block" : "block"}` width={width} height={height} src={href ?? "/blueberries.png"} />
